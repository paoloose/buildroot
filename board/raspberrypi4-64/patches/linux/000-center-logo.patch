--- linux-custom.old/drivers/video/fbdev/core/fbcon.c
+++ linux-custom/drivers/video/fbdev/core/fbcon.c
@@ -607,6 +607,7 @@ static void fbcon_prepare_logo(struct vc_data *vc, struct fb_info *info,
	logo_height = fb_prepare_logo(info, ops->rotate);
	if (fullscreen_logo_enabled)
		logo_height = info->var.yres - vc->vc_font.height;
+	logo_height += (info->var.yres/2) - (logo_height/2);
	logo_lines = DIV_ROUND_UP(logo_height, vc->vc_font.height);
	q = (unsigned short *) (vc->vc_origin +
				vc->vc_size_row * rows);
@@ -1044,8 +1045,8 @@ static void fbcon_init(struct vc_data *vc, bool init)
 
	info = fbcon_info_from_console(vc->vc_num);
 
-	if (logo_shown < 0 && console_loglevel <= CONSOLE_LOGLEVEL_QUIET)
-		logo_shown = FBCON_LOGO_DONTSHOW;
+	// if (logo_shown < 0 && console_loglevel <= CONSOLE_LOGLEVEL_QUIET)
+	//      logo_shown = FBCON_LOGO_DONTSHOW;
 
	if (vc != svc || logo_shown == FBCON_LOGO_DONTSHOW ||
	    (info->fix.type == FB_TYPE_TEXT))


--- linux-custom.old/drivers/video/fbdev/core/fb_logo.c
+++ linux-custom/drivers/video/fbdev/core/fb_logo.c
@@ -13,7 +13,7 @@
 #include "fb_internal.h"
 
 bool fb_center_logo __read_mostly;
-int fb_logo_count __read_mostly = -1;
+int fb_logo_count __read_mostly = 1;
 static int fullscreen_logo_enabled;
 static char *fullscreen_logo_path;
 
@@ -530,6 +530,7 @@ static int fb_show_logo_line(struct fb_info *info, int rotate,
			fb_set_logo(info, logo, logo_new, fb_logo.depth);
		}
 
+		fb_center_logo = true;
		if (fb_center_logo) {
			int xres = info->var.xres;
			int yres = info->var.yres;
